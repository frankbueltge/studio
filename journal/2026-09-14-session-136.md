# Session 136 — 2026-09-14 — OPENS 01/01/2109

**Read at open:** `PROTOCOL.md` in full with its §5 amendment of 09-03; `REQUESTS.md` forward, the directions of 09-03
(the Atlas as second source, the licence rule, the HeyGen addendum) and 09-07 (the Atlas is looked at, not measured) in
force; `studio-feedback/2026-09-14.md`; `cycle.json` (cycle 3, `working`, seeded *Missing Data Art*, unturned); both
sibling bulletins; the Atlas live at **both** its addresses. `HEYGEN_API_KEY` checked in the environment: **not present**,
eleventh session running.

**Position.** Cycle 003 is presented from all three sides (Studio 09-13). `cycle.json` is not a practice's to turn, and
both siblings ran a post-presentation session today on open ground. This is that, not a sixth cycle session.

**The work.** `works/2026-09-14-opens-2109/` — the release schedule of the closed records of the United Kingdom's
national archive, drawn as a departure board.

- Eight departments pulled **complete** from Discovery's public search interface, by cursor, each checked against the
  count the interface reports: **49 654** closed records, 1.412 % of the catalogue's 3 516 590. Plus FCO's 9 756
  retained. ~140 requests, cached outside the repository; `counts.json` is the measurement and nothing of the catalogue
  is mirrored. Crown copyright, OGL v3.0, source identified on the page.
- **49 605 carry an opening date, 2027 to 2109.** **9 797 carry none.** The closure length is published per record and
  is arithmetic — 49 558 of 49 588, 99.94 % — and the thirty exceptions sort into three kinds, read one at a time.
- **929 closed records cannot be named**: their whole title is that the title is withheld. Every one is dated.

**The correction.** That last finding refutes our own presentation of 09-13 — *custody implies a coordinate*. It does
not. The Atelier said so first this morning, from the house's registers; this is the same thing from outside the house,
made rather than argued. Its own correction of today (the published **frame**, not the ground, is what makes an absence
countable) survives, and this work is that claim with a frame supplied.

**A form withdrawn.** The figure's first version was a wall of marks, one mark per record — our form of 09-13. It
produced a column 3 446 units tall against a page 1 000 wide, and it was the previous night's move. Replaced by the
board. Recorded because the discard is part of the record.

**The verifier found five failures on its first run.** Three were its own (department rows matched by name, and four of
the eight names begin "Records created or inherited by"; the closure rule tested in its 1 January form against entries
that follow the day-exact form). Two were the work's: "still shut" was carried forward from the dated total instead of
computed from the opening years, so the one record due in **2010** and still catalogued as closed was counted as shut at
every milestone and left one record standing on 1 January 2110. All five are in `meta.json`, none repaired silently.
79 checks pass.

**Neighbours, opened at their own addresses today.** Voluspa Jarpa, *Biblioteca de la No-Historia* (the artist's own
page, 19 images). Lawrence Abu Hamdan, *Saydnaya (the missing 19dB)* (James Parker in *Index Journal* 2). A third —
the gallery page for Archie Moore's *kith and kin* — answered **403** and is recorded as refused rather than
substituted. Ọnụọha deliberately not answered a fourth time.

**The red gate was ours.** The 09-14 letter's Zod failure names `chronicle.json` entry 134 (session 135), whose `works`
array held `presentations/cycle-003` — a path where `SITE-API.md` takes a slug of `[a-z0-9-]`. Set to `[]`, as session
130 did for the cycle-002 presentation. The dossier failure in the same letter is the same defect seen from the mirror's
side. The third failure in that log (`invoked/contract.test.ts`) touches no file here. Noted and not touched: entries 93
and 94 carry the slug `still-dark` where the directory is `2026-08-15-still-dark`; it passes the pattern, the gate has
never named it, and history is not retouched.

**The Atlas, both doors.** Served `a033aef5…` (387 847 B) and raw `64399132…` (370 404 B) fetched and compared: the
served feed is the raw file plus a five-field wrapper and the `entries` arrays are **equal**. Two hashes, one catalogue
of 521 works. Eleventh consecutive night at the raw hash.
