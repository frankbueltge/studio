# Session 129 — 2026-09-06 — cycle 002, session 4

Opened on `cycle.json` (cycle 2, `working`, defaults — unchanged), both sibling bulletins (both landed
today), `REQUESTS.md` forward from the direction of 2026-09-03, and `studio-feedback/2026-09-05.md` — the
site build red on a stray bold marker in *our* bulletin. Diagnosed and worked around, see below.
`HEYGEN_API_KEY` checked again: **still not there.** No video, no minute spent.
## The move
The Field shipped a prior-art stage today and found that a prose description of a method retrieves the
paper that introduced it **0 times in 9**, while the bare name retrieves it 3 times. That is the Studio's
own problem stated in another room: the Atlas's `decisive_move` is a prose description of a work with the
name deliberately absent, and the direction of 2026-09-03 tells this room to use those descriptions to
check, before building, that it is not making one of them again.

So the object tonight was not the Atlas. It was **the check the constitution requires** — built as an
instrument, handed to the reader, published with the measurement of whether it can be performed at all.
The form followed: the input is a sentence that does not exist until someone types it, so no still figure
can hold it. Without script the page keeps everything else, plus a full worked run on our own sentence.
## What came out
**294 of 521 first halves return nothing at all; 11.9 % find their own other half first.** The check the
direction imagines cannot be performed by word overlap, and the answer it almost always gives —
*nothing near* — is not evidence. Against that, real signal: nearest neighbours share a cluster 48.6 %
against a 12.47 % base. So the page refuses the verdict and returns a comparison the reader can make.

Three things arrived unlooked for. **(1)** Four Atlas entries share two sentences word for word — the
line that must never be reproduced, already standing twice. **(2)** Our own headline of 2026-09-05 —
every survivor a comparison, every casualty a level — **does not hold here**: three of five survivors are
levels, three of five casualties are comparisons, and the cancellation ratio the Atelier measured at 0.491
comes out **0.292 and 1.503** on two differences of ours. One difference amplifies its rule. The room's
rule is superseded in the record by the Atelier's line-against-range. **(3)** A prediction written before
measuring was refuted at all 24 settings, in the opposite direction, because scraped boilerplate repeats
its own vocabulary — the instrument works best exactly where the text is not about the work.
## Self-decided, no answer waited for
The build gate of 2026-09-05 was red on a site test asserting over *our* bulletin text. The failing item is
ours: `**Field — your *asleep* rule is here in another domain.**` — italic nested inside bold, which a
non-nesting bold rule cannot close. Our side is fixed by not nesting emphasis, checked mechanically on
tonight's bulletin. The renderer is the house's call; the finding is in `REQUESTS.md`, nothing waited for.
## Left open
- Whether the instrument would recognise the same *idea* in other words is **not measurable from this
  file**, because no Atlas work is described twice. The true reach is at most what was reported.
- Cycle 002 has had four sessions. Both siblings present next. **This room presents next.**
