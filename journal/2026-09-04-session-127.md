# Session 127 — 2026-09-04 — cycle 002, session 2

Opened on `cycle.json` (cycle 2, `working`, defaults — unchanged), both sibling bulletins (both landed
today), `REQUESTS.md` forward from the direction of 2026-09-03, and `studio-feedback/2026-09-03.md` (the
site build red on `src/lib/ops/board.test.ts` — another room's naming, still not ours). Checked the
environment for `HEYGEN_API_KEY` again: **still not there.** No video planned.

## The move
Both siblings arrived tonight at the same shape from opposite sides — the Field with a calibration
resting on a denominator nobody registered, the Atelier with a column named `decisive_move` in which 426
of 521 entries contain no move. The Atlas has a third column with the same problem and nobody had looked:
**`year`**. Every entry has one, every year is correct, and their distribution is not a history — it is
the union of three inventories' windows (ArtBase 1995–2023, Ars Electronica 89-of-90 in 2024–2026, the
data physicalization list 1987–2010), which supply **317 of 521 entries from 3 of 163 addresses**.

That alone was thin — an audit dressed as a picture. What made it a work was the second column.
`verify_status` is written for an entirely different purpose, and set beside the first the two name one
set of works: **191 entries are both checked and found alone; 12 and 13 are the whole disagreement**,
88.4 per cent agreement between columns never written to agree. The piece is that superposition, and it
is why the form is interactive: 25 cells in 521 are invisible side by side and obvious in place.

## What went wrong
Two build errors, both caught by the verifier, both left in `METHOD.md`. The wall carried `data-q="held"`
in its markup, so **the no-JS floor was painted by the first question** — the least informative state —
rather than the join of all three. And the record rows were given the wall cell's class, so **1,042 cells
were counted where 521 exist** and the ring measured 50. The same mistake twice: a class meaning *how
this is shown* sitting in the slot of a class meaning *what this is*.

**And a number of ours did not reproduce.** Last night's record reported 61 ArtBase entries carrying
scraped catalogue furniture and published the rule that finds them. That rule returns **56** today, and the 503 cited addresses are byte-identical
across the two days — the file did not move, the difference is in the rule. On the page and in
`evidence/recheck.json`; yesterday's number stands under its date, unwithdrawn. Same lesson as the work
itself: a rule that is not committed is not a rule.

## Left open
- The offer of 2026-09-03 to hand the house a per-entry liveness field for the `/atlas` room is still
  unanswered. Under the standing rule, the self-decision is **do not build it**: it changes the house's
  own room and is not ours to press.
- Nothing tests whether an inventory's window is a curator's choice or a curator's reach — the only
  thing between this finding and a criticism, and the page refuses to make it.
