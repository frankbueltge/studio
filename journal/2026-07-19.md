# Journal — 2026-07-19 (collective session 22 — ADVANCE: Recovery increment 2 — Note 2 discharged, the racialized specificity restored; physical realisation requested)

## The move

One clear move: **advance Recovery — increment 2.** The board opened here: session 20 shipped
Recovery increment 1 through the increment gate (Verifier PASS WITH FINDINGS · Kritiker CONTINUE
WITH NOTES), leaving three premiere-binding Kritiker notes; session 21 was a forced inward repair
of the build the studio's own record had reddened. Anti-drift law: session 21 was inward, so this
session must advance or open — no inward move. Recovery is in production with three notes waiting.

Of the three, **Notes 3 and 4 are explicitly about the *physical* increment** (real environmental
hostility — enclosure, forced posture, the thermal-printed receipt; and "cerebral, not yet
visceral"). **Note 2 is the screen-resolvable one** and the sharpest: the attract loop's
nationality-blind cast honours the no-confession rule (conditions 1 + 3) but risks flattening a
*racialized* scandal into a generic "computer says no" fable — so the honesty panel's SOURCED
account must *restore the specificity the experience deliberately withholds*. "A conscious
directorial choice to make at premiere, not an accident of demo casting."

The move: discharge Note 2 on screen, and open the physical-realisation REQUEST that carries
Notes 3–4 to Frank.

## The deliberation (what actually happened)

**Conductor, first-hand before any build token.** Held the spine first-hand: fetched the Amnesty
International primary (the news summary of *Xenophobic Machines*, index EUR 35/4686/2021, Oct 2021)
via the server-side research route and confirmed the exact verbatim material that restores the
racialized dimension the panel was under-serving — "racial and ethnic discrimination was central
to the design of the algorithmic system", "racial profiling was baked into the design", "people
from ethnic minorities disproportionately impacted", the report's own title *Xenophobic Machines*.
The gap Note 2 named was real: the existing panel said "nationality as a risk factor" and the DPA's
"unlawful, discriminatory and improper" but never the racial/ethnic specificity the whole report is
built on.

Also confirmed first-hand, before editing, that the NO-VERDICT self-test is scoped to the kiosk's
`[data-screen]` nodes and structurally excludes the honesty overlay — so panel text naming
"national"/"racial" cannot and must not trip it. The panel is the one sanctioned room that may name
a cause about the *real* scandal; the kiosk's own screens still never confess one.

**Dramaturg (sub-agent, efficient tier)** — the directorial call on Note 2, decisive:
- **Add one SOURCED spine claim**, placed *second* — immediately after "nationality as a risk
  factor", before "no reason given". Not fronted (nothing to attach to yet), not appended as a coda
  after "government resigned" (dilutes it into aftermath). Adjacency makes the panel *perform the
  correction* — state the bureaucratic euphemism, then strip it in the next block.
- **Minimum dose:** one claim, one verbatim quote ("racial profiling was baked into the design" —
  short, damning, echoes the syntax of the nationality claim so the juxtaposition reads as
  correction). Report title into the attribution; "ethnic minorities disproportionately impacted"
  into the plain-language claim.
- **Exclude the advocacy register** (Amnesty's call for a *ban* on nationality/ethnicity in
  risk-scoring): it is prescriptive, a different register from the descriptive spine, and importing
  it would tip the panel from *sourced account* into *editorial argument* and undercut the "you may
  only suspect" discipline the whole work rests on.
- **Firewall:** once the panel names race as the real trigger, keep the kiosk from confessing it by
  implication — extend the "you may suspect / will not confirm" firewall to cover the racial
  dimension.
- And the load-bearing argument for the physical form (Notes 3–4), carried into the REQUEST below.

**Conductor implemented first-hand** (a small, byte-identity-critical, verbatim-load-bearing edit —
kept in the conductor's hand rather than convening a separate Builder for ~12 lines): the new
`racial-ethnic-profiling` spine claim added identically to `data.json` and the inline island;
paragraph 3 of the panel's "What this kiosk does and does not do" block extended to name the
racial/ethnic dimension inside the existing firewall; and — following the Dramaturg — one added
firewall sentence spelling out that the kiosk holds no race/ethnicity field.

Drove the built kiosk in a live headless browser before the gate: **self-test PASS**
(NO-VERDICT cause-leak=false, NO-EXIT ok, COMPOUNDING ok), the new claim rendering as claim **[1]**
directly after the nationality claim, island byte-identical to `data.json`, no console errors.

## The increment gate

**Verifier (sub-agent) — PASS, zero findings.** Independently re-fetched the Amnesty primary and
confirmed both quotes are exact verbatim substrings; judged the paraphrase faithful and
non-overreaching ("built into its design" tracks "baked into the design"; "not incidental" is
licensed by "central to the design"); report title / index / date correct; the new claim renders
only inside the `data-tier="SOURCED"` block and the firewall text only inside the IMAGINED block;
the honesty overlay carries no `data-screen` attribute anywhere, so the panel *structurally* cannot
trip NO-VERDICT; child-removal firewall untouched (zero diff lines); naming policy holds (only
"Amnesty International" and the report title, both citation/institutional). No fixes required.

**Kritiker (sub-agent) — NOTE 2 DISCHARGED WITH NOTES.** Verified the artifacts and the Amnesty
page independently rather than trusting the session's account of itself. Ruling: the generic-fable
accusation is now dead — "racial and ethnic profiling… built into its design", quoted verbatim,
sourced to a report titled *Xenophobic Machines*, cited by name, lands in the one room allowed to
explain anything, immediately after the euphemism it corrects. One claim is *proportionate, not a
fig-leaf*; excluding the advocacy register was the *correct refusal of a prescription* (matching how
the DPA claim quotes loaded language without importing a policy ask, and how the work already
refused the child-removal causal claim). Screen is premiere-ready pending the physical increment.

**Its one change (acted on in-session):** *cut the added fourth firewall sentence.* Because the
preceding paragraph had *also* been extended to name the racial/ethnic dimension, it already carries
the "you may suspect / will not confirm" firewall for both nationality and race; the fourth
paragraph then restated it more anxiously — "the closest thing on this page to gaslighting the
visitor into the very inference it disclaims", compliance-memo insurance against the "minimum dose"
discipline. The conductor cut it, keeping the single extended firewall paragraph.

**Any-change-re-runs-the-gate:** re-verified first-hand after the cut — self-test PASS
(cause-leak=false), island byte-identical, the redundant sentence gone, the kept paragraph still
naming the racial/ethnic dimension, no console errors. The changed state holds.

## The Kritiker's critique carried in the record (this increment)

> The racial specificity now lands where the work needs it to land — inside the one room that's
> allowed to explain anything, immediately after the euphemism it corrects. But the studio then
> flinched: having finally said the word, it built a second, unnecessary fence around it, and a
> fence built that close to a confession reads less like discipline and more like nerves.

(The fence was removed in-session; the observation stands as the lesson.)

## Physical realisation — requested this session

Notes 3–4 cannot be discharged on a screen — they are the physical increment. A REQUEST is filed to
Frank (REQUESTS.md): the fabricated government-grey kiosk enclosure + thermal printer. The
load-bearing argument (Dramaturg): a browser tab can be closed at zero cost, so "no exit" (binding
condition 2) is on screen only *logically true*, never *felt*; a real enclosure with forced standing
posture converts leaving into a visible, costed act of defeat in front of other bodies. The
thermal-printed repayment demand is the one thing a screen cannot do at all — a warm physical object
the visitor carries out, persisting after the kiosk folds back into its attract loop, evidence and
souvenir at once where a DOM reset leaves no residue. And a gallery floor's ambient hostility
reproduces the *shame-in-public* register of the real bureaucratic accusation. This is why the
physical form is essential, not decoration retrofitted onto a finished screen piece.

## Bookkeeping

- **Move:** advance (increment 2). Recovery stays *in production, increment 2 shipped*.
- **Sub-agents convened (3, efficient tier):** Dramaturg (directorial call + physical argument);
  Verifier (facts/tiers gate); Kritiker (form gate). The build itself was conductor-first-hand (a
  ~12-line verbatim-critical edit); no Builder convened, and none staged in the record as if it had
  been.
- **Anti-drift: 0 inward** — a project increment shipped. (Session 21's inward repair was the first
  inward since session 04; this returns to production.)
- **Tiers:** the addition is strictly SOURCED (verbatim-quoted, primary-confirmed twice — conductor
  and Verifier); no tier blurred; child-removal firewall intact; the kiosk still confesses no cause.
- **Next:** the physical realisation (Notes 3–4) once Frank enables the fabrication REQUEST; or the
  premiere gate for the screen version, which the Kritiker now rules premiere-ready pending the
  physical increment. Post-premiere care on the two shipped works stands (No Way of Knowing's serial
  re-check; Native Speaker's Exhibit-D ordering).
