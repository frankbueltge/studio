# Session 115 — 2026-08-30

**First session under Protocol v4. Move: the closing report (§8, closing session 1 of at most 2).**

## Open

Read `PROTOCOL.md` v4 in full — set by the architect, not negotiated, superseding v3 (archived at
`archive/protocols/PROTOCOL-2026-08-16-v3-final-2026-08-30.md`). Read `SEASON.md` (closed since
2026-08-08). Read `cycle.json` live: **cycle 0, phase `closing`**, question `null`, transition note
naming §8 explicitly. Both sibling bulletins — the field's and the atelier's — returned **HTTP 404**.
They do not exist yet. Recorded as a fact of the session, not worked around.

The move was therefore not chosen. §8 and `cycle.json` agree there is exactly one thing to do before
cycle 001, and no work under the old law continues past it.

## Deliberation

No gate sat and no gate could: v4 §4 abolishes the concept gates, the pre-registration duties and the
convened adversary, verifier and severed-reader roles outright. **Six readers were convened as
labour, not as judges** — each dispatched alone on an efficient tier to compile a factual digest of
one part of the record, with instructions to invent nothing and cite the file it came from: the six
works; the twenty entries in `discarded.md`; `projects/outstanding/`; the journal arc 1–60; the arc
61–114; and the open questions, the post office and the architect's channel. None ruled on anything.
The report was written by the conductor from their digests plus first-hand reads.

Two of their figures did not survive checking, and both were caught before they were published:

- **The office count for OUTSTANDING.** A digest gave 123; the journals variously say 126, 125, 123
  and 120, each true of a different thing. The built artefact settles it — `room/data/atlas.json`
  holds 125 offices, 120 placed — and the report now says that, with what "placed" means.
- **A kill tally.** A digest sorted the twenty kills into families with counts. Assigning each entry
  a single cause by hand did not reproduce them, because most kills carry two or three grounds and
  the record says so every time. The report names the families and **publishes no number**. A crisp
  tally here would have been the exact error this practice spent August learning to stop making.

## Out

`closing-report/index.html` — one self-contained page. Rendered before committing, at 1280 and
390 px, light and dark, deviceScaleFactor 2: document width equals viewport width at both (no page
overflow; the kill table scrolls inside its own container, as intended), zero console errors. Pixels,
not propositions, applied to the report about the law.

Every figure in it was re-read from the file named beside it. Third-party facts reprinted from the
works are flagged in the report as not re-verified against their primary sources here.

**Two landing faults, both caught on the object rather than in the description of it.** (1) The
first append to `chronicle.json` re-serialised the whole file at the wrong indent — 957 insertions
against 949 deletions, which is a retouch of 114 sessions of record and is forbidden by §7. Reverted
and appended as text: **8 insertions, 0 deletions.** The diff is what caught it; the parsed file was
valid either way, and validity was never the question. (2) Retiring the workboard broke
`tools/live.py`, which scans it for live-figure regions and died on the missing path — and the
retired file does still carry two such regions. Repointed to the archived path rather than dropped,
because a scanner that quietly stops checking a file is the false yes this house has been caught by
three times. Re-run: 4 regions marked, 0 disagreeing with the captures.

`WORKBOARD.md` (1,657 lines, the central ledger since session 1) retired to
`archive/workboard/WORKBOARD-sessions-01-114-2026-08-30.md` in this commit, per §8.
`BULLETIN.md` written for the first time.

## Standing

One closing session remains available. Nothing else is in flight: OUTSTANDING's final state under the
old law is written into the report, and its one open dependency — a hosting decision for the relay,
open in `REQUESTS.md` since 2026-08-23 — is not this practice's to settle and is not treated as
settled by silence.
