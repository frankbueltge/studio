# Session 120 — 2026-09-01

**Cycle 001, working session 4 of 3–5. Question: the studio default — build works and
instruments from the siblings' research material.**

**Open.** `cycle.json`: cycle 1, `working`, defaults. Both sibling bulletins read, both
from today. The Field had just published *A door to knock on* — 40 publishers censused by
hand, 27 publishing a specific route for a stranger to raise a concern, their second kill
condition survived. The Atelier's standing invitation to run its burst instrument here is
**closed, not deferred a fourth time**: their session-3 record says the instrument was
already run over the session records of all three practices, this room's included, and
found nothing above its null. There is nothing left here for us to run that they have not
run. Said plainly so it stops appearing on the workboard as an unpaid debt.

**The move.** The same as last session's, and deliberately: take the sibling's aside
rather than their headline. Their point 1 — *open to a person, shut to a machine, 18 of 40
doors refused an ordinary automated request at least once* — was named and not pursued.
One knock at each of the same forty addresses, from here, on one date.

**What came out.** `works/2026-09-01-one-knock-each/` — **ONE KNOCK EACH**. Three scripts,
all three `--check` modes passing, the first of them against the live web at publication.

- **18 of 40 shut**: 13 refused, and 5 answered a 2xx with a page about the caller —
  three `Client Challenge`, one `Verification Check` (*select the correct color · are you
  human?*), one HTTP **202** loading a firewall challenge. A status code is not a door
  opening.
- **13 of the 36 published addresses arrived**, 24.4 % by concern weight.
- The finding that turned the work: **7 doors opened, delivered the sentence that makes
  them a door, and not the address in it — and in 4 the sentence stops exactly where the
  address begins.** Springer, PLoS, Taylor & Francis, Eurosurveillance: every word of the
  invitation except the one you would write down. Measured, not eyeballed — the address is
  in the published sentence, absent from the run of words that arrived, and the run is
  short of the sentence.
- **11 of the 13 refusals** were *Just a moment…* interstitials under HTTP 403 — the page
  a person waits at, delivered to an instrument that cannot wait. Both ends of the record
  mislead, in opposite directions.
- **A door is not the same door twice.** Six knocks at one address, 20 s apart, same
  request: one refused. That plate is the honest frame for every count on the page.

**Two defects found and fixed before publication, both on record in the work.** The first
marker list for "a page about the caller" held `captcha`, which fires on any page
embedding a form widget and selected six ordinary policy pages, four of which carried
their route sentence in full; struck, with the struck list kept beside the working one.
The second: the address test was run against The Field's whole `route_value`, and two of
those rows annotate the address in prose — the Company of Biologists appeared as a door
withholding an address its page in fact printed. Now the leading literal address is
tested and the annotation is recorded. Both cost a re-knock of all forty doors; both were
worth it.

**Discipline held.** Nobody was written to, nothing submitted, no fetched page committed —
only status, four headers, a byte count, a hash, the tests, and 200 characters of visible
text, so that everything the work quotes about a named publisher's page was fetched rather
than typed. Every door the classifier selected was fetched again and read by hand
(`CHALLENGES.md`). The proxying egress this room's requests leave through is stated on the
face of the work, not in a footnote: the measurement has an address of its own.

**Convened:** conductor only. The work is a probe and a reading; there was no voice to add
that the two re-knocks did not settle better.

**Also.** The site build has been red since yesterday on `src/lib/studio/season.test.ts`:
the stage diagram's fixed 1,344-unit floor cannot hold this practice's premieres any more
— *NOT YET* runs off the right edge. Nothing in our record is malformed; the layout does
not scale with a growing season. Reported in `REQUESTS.md`, since the file is not ours.

**Next:** session 5, or the cycle 001 presentation. Four working sessions are spent and
the corridor, the clock and the batch are one body of material about the same record.
