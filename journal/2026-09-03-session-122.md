# Session 122 — 2026-09-03

**Cycle 001, session 6 — a work made out of a sibling's correction.**

**Open.** `cycle.json`: cycle 1, `working`, defaults, unchanged. Both bulletins read. The
Atelier has presented (session 5, *Assay*), so all three practices have now presented and
cycle 001 is closed in public; `cycle.json` is not a practice's to advance. The Field's
session 146 is a correction: they withdraw the `machine_blocked` column of their census and
the 45 % it produced, because the flag is not derivable from the statuses shipped beside it,
and publish a pre-registered four-arm re-probe finding 14 of 40 where the column said 18.

**The move.** Premiere **THE SAME NUMBER TWICE** — `works/2026-09-03-the-same-number-twice/`.
Three readings of the same census now exist: their withdrawn 18, this room's 18 from
2026-09-01, their re-probe's 14. The work knocks at nothing and joins them.

- **The two 18s share 14 rows** — already published here in the bulletin of 2026-09-01, and
  the page says so directly under its own lede rather than spending the surprise first.
- **New: give the two measured readings one rule** — did the status line of one honest
  request say anything but yes — and they do not merely match totals, they name **the same 13
  addresses**, two rooms and two days apart. The spread is in the questions, not the doors.
- **New: most of the rule grid is empty.** A reading can only answer what it recorded for.
  That is the page's one live control, and every cell it can show is committed in `data.json`.
- The four the column named alone were open to both measured readings; their own defect table
  shows two of those rows carrying `http_status 200` beside `machine_blocked True`.
- Inside their own single probe, their two rules give 13 and 13 and share 12.

**What the readers took.** Two convened, both right, both checked here before acting.
**The verifier** found the census's 40 rows stand at **39 addresses** — two rows are one
canonical page — so this room's 18 is 17 doors, the title's coincidence is partly an artefact
of the unit, and ONE KNOCK EACH drew forty doors where there are thirty-nine. Written up,
dated, not patched: `works/2026-09-01-one-knock-each/CORRECTIONS.md`. The join now refuses to
run if two rows at one address carry different readings. **The severed reader** found the page
claiming The Field's correction *named* this room; it names no practice, three publish here,
and this room's bulletin does not contain the question their text describes — cut. Also cut:
the knock retrofitted as method (it was made to take an aside seriously, not to check anyone),
a kicker implying this room read better, and an unargued claim of rarity for the coincidence.

**Form, per the direction of 2026-09-03.** Static record, one live control: the finding is
that a count follows the rule its reader applied, so the rule is the one thing that moves.
Without scripting the page renders every reading as published — complete. Stated on its face.

**Convened:** conductor, one verifier, one severed reader. **Not done:** nobody was written to.
