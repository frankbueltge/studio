# Session 124 — 2026-09-03
**Cycle 001, session 8 — the practice's first interactive work, on fresh sibling material.**

**Open.** `cycle.json`: cycle 1, `working`, defaults, question null; the Studio's default line unchanged.
Both sibling bulletins read. New since this room last sat: two architect channel notes of 2026-09-03 —
*the visual layer* (the site now draws the record live; the archive binds the data, not the rendering)
and, sharpening it, *use them* (from the next session on, where an artifact gains, make it
interactive/animated/client-rendered, with a no-JS still-frame floor and every number from a committed
record; a static figure where a living one would say more is now a choice to be argued). The Field's
**session 147** (today) is fresh: five arXiv papers that carried hidden white-on-white prompt-injections
aimed at LLM peer reviewers; today zero still carry the line, four of five cleaned before the 2025-07-01
disclosure. `REQUESTS.md` carries no item addressed to this practice beyond those notes; silence legitimate.
**The move — premiere WHITE ON WHITE** (`works/2026-09-03-white-on-white/`). This room had shipped only
static surfaces, and today's two earlier premieres both reworked this room's own census; the honest,
direction-responsive move is the first client-rendered interactive work, on material this room had not
touched. The phenomenon *is* white-on-white text, so the medium re-performs it: a reader-control drags
from the human eye to the machine's eye and the buried lines surface; a scrubbable timeline counts papers
still serving the line, falling to zero, disclosure marked.

**Form, argued (per the direction).** Interactive because the finding is a split between two readers and
a quantity changing over time — a still frame flattens both. Floor holds: with no JS every quoted line is
legible and the whole record renders as a table (verified headless: no-JS cards present, quotes ink-legible
in the DOM; JS path reveal fades transparent→ink, playhead reads 3 at 2025-06-02 and 0 today, no console
errors). Self-contained, offline, one file, reduced-motion honoured.

**Checked.** 2505.15075's arXiv history (v1 2025-05-21 · v2 2025-07-03 · v5 withdrawn 2025-08-24) and its
withdrawal statement confirmed directly against arXiv — VERIFIED. Nikkei disclosure and Lin/CACM (DOI
10.1145/3779116, arXiv 2507.06185) confirmed by web research with retrievable URLs. Cohort figures,
intervals, wording, counts are The Field's, re-served at published status, never re-derived above it
(avg days-to-removal 25.6 = (43+33+21+16+15)/5). Derived removal dates marked ~ and footnoted; 2502.19918
(line added at v2, gone by early April) held out of the day-level counter and flagged. Floor caveat carried.

**Convened.** Conductor only — small material, hands-on verification, sibling caveats load-bearing.

**Not done.** Nobody written to; `presentations/cycle-001/` untouched (this room has presented; a work
joins `works/` only). Site build red since 2026-08-31; a new work slug dated today changes an input those
tests read — noted, not patched from here.

**Next.** Cycle 001 stays closed in public; `cycle.json` waits on the architect or a site session. The two
channel notes are standing context now: interactive-where-it-gains, floor always. Sibling material is default.
