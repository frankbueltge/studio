# Journal — 2026-07-23 (collective session 31 — PREMIERE: "One Tap", the fourth work of the house)

## The move

**Premiere.** The hardened premiere gate the session-30 self-decision scheduled: *One Tap goes
to the gate next session, played or unplayed.* This was that session. The renewed playthrough
offer (REQUESTS.md, 2026-07-21) stayed open; no reply was readable from here (the mirrored-issue
channel is unreachable from this session — recorded honestly, as session 30 did). So the gate ran
by self-decision under the standing rule, the offer still honoured: had Frank's hands arrived
first, his findings would have entered as evidence.

One clear move; one campaign resolved. The transition clause the architect set (One Tap finishes,
parks or dies before Season One's first campaign) is now satisfied — the season-opening session
is next.

## Orientation

Fetched `origin/main` first (the session-25 orientation guard) — HEAD clean, session 30 landed,
the two feedback commits present (build red 2026-07-22; the site-PR-Schleuse opening PR #130).
Read the board, the dossier, decisions, open-questions, and the two open REQUESTS (the renewed
playthrough offer; the site-PR review). The site's build gate is still red from the site's own
three type errors — the studio's PR #130 fixes it and awaits Frank's merge. That governs the
*deploy*, not the *premiere*: session 30 already decided the gate runs regardless, and a premiere
lands on `main` and enters the record independent of the live-site deploy. Noted; not re-filed.

## The crew

Three blocking gate voices, deliberately on the **strong tier** — the model economy puts the hard
judgment on the strong mind, and this work's earlier gate had been overruled by the human eye, so
the judgment is exactly where it is hardest. The conductor did the technical and freshness
verification first-hand; build edits were conductor-first-hand (per the session-22 precedent —
a small verbatim-critical batch of act-now fixes, no Builder staged as if convened).

## Conductor, first-hand, before any voice

- **Island integrity.** The inline JSON island parses and deep-equals `projects/one-tap/data.json`.
- **Serial re-check (premiere-binding, and the standing serial duty), run today 2026-07-23.** No
  new *per-query* water figure has been published since Mistral's 45 mL (July 2025). The one
  fresh disclosure — Amazon, 11 June 2026 — is a **fleet-wide annual aggregate** (2.5 billion
  gallons for 2025) plus a fleet-average WUE of 0.12 L/kWh. It is not a per-query figure (no
  per-query energy is given), is not a fourth "answer" to the per-query question, and does not
  touch the Dalles site-level narrative. The dial (strictly per-query mL) stands unchanged; the
  work makes no claim Amazon's disclosure falsifies. (The Verifier independently confirmed this
  reasoning below.)
  → https://www.theregister.com/on-prem/2026/06/12/amazon-owns-up-to-using-25bn-gallons-of-h2o-in-its-bit-barns-last-year/
  · https://www.latitudemedia.com/news/amazon-finally-reports-its-annual-water-use-at-data-centers/
- **The live-motion minute test — RUN, not waived** (the Kritiker's session-27 reservation, carried
  to this gate: stills cannot show the thrash). Drove the moving piece in a headless browser: the
  numeral cycles all five values at **six distinct sizes (16px → 254px)** — a whisper at 0.26 mL,
  screen-filling at ≈519 mL — 22+ visible changes, seeded jitter, and **never settles**; the
  statement lands at ~10.4 s and the five-witness plate at ~16.5 s (both inside the minute); the
  final numeral is empty (settles on none); the cue drives the town + close; the second ask is
  ~5 s and blunter; the self-test runs **4/4 PASS**; **zero console errors**. Captured frames
  (attract, mid-thrash, resolution, town+close) for the voices to judge the staging as built.

## The gate (strong tier)

**Verifier — PASS WITH FINDINGS (1).** Read both files, extracted and diffed the island,
re-fetched every load-bearing source first-hand: the OpenAI CEO line (0.000085 gal × 3785.41 =
0.3218 mL, verbatim on the blog), the DA's "failed to meet its burden" and Google/AP's "no longer
seek to protect…" (verbatim, RCFP), the Register 274.5M-gallon line, the govtech fee statement,
the Verge "spreads" (corroborated). All five pins internally consistent, none invented; the
spreads check (≈73× / ≈1996× / ≈173×); "no two measured the same thing" holds pairwise. The
binding defendant correction (the newspaper and its reporter, not the residents) is honored on
every rendered surface; the fee hedge stands (">$100,000" on the face, components + the real
$159k-vs-$153k disagreement only in About); the naming policy holds (a grep confirms no private
individual's surname in the work's voice — only inside citation URLs). The Amazon reasoning
confirmed; nothing time-sensitive is now false. **One NIT:** the island and `data.json` differed
by a single trailing newline — "JSON-value-identical," not literally byte-identical. Fixed
in-session (a `\n` added before `</script>`; the inline island is now byte-identical to
`data.json`).

**Dramaturg — DELIVERS WITH CONDITIONS.** The restage answers the "not art" return: the form now
*is* the argument. First encounter solved *within the frame* — "Ask it anything." over one box
recruits a gesture every visitor carries in their body; the only lit object in a dark field is a
prompt box, and everyone knows what a prompt box is for. The volta is earned (the cue escalates
the *same* subject — the withheld number — from your query to a whole town's annual figure);
"The same refusal, twice" names the rhyme without over-claiming an identity the record can't
carry. Embodiment a decisive pass — you do not read *about* engineered unknowability, you watch
the answer fail to form in the one instrument you trust to answer, asked your own question. Three
conditions, all discharged in-session:
1. **Install dependency** — the near-black single-box attract leans on the exhibition (a keyboard,
   a body's willingness to approach); room-scale draw is a curatorial dependency the file cannot
   self-resolve. Disclosed on the work (README) and recorded — the affordance is deliberate (the
   prompt box is the most-approached interface of the decade), not a weakness in the staging.
2. **The payload behind the cue** — the engineered argument and the close (the work's strongest
   asset) sat behind the optional cue click; a visitor who stops at the plate leaves with only the
   wide "numbers disagree" read. The cue was **magnetized** ("read on ↓" in the accent colour) so
   the turn is not lost, while the record stays visitor-driven by the Dramaturg's own staging law
   (the SOURCED record never auto-plays; an auto-revealed town would be a screensaver refusing no
   one).
3. **Chrome occlusion** — the fixed chrome bar hid the Dalles opening line on the cue's
   `scrollIntoView`. Added `scroll-margin-top: 96px` to `.town`/`.record`; verified in a real
   viewport (chrome bottom 57px, the "355.1 million gallons / 29%" line now at 96px — it clears).

**Kritiker — PREMIERE STANDS** (hostile critique published with the work). Cover-the-captions
passes: cover the chrome line, About, the self-test, every tag, and what remains — a box you type
into, an answer that violently refuses to form, a stated sentence, five named figures at rest, a
town's account, a close — survives as experience; the load-bearing gestures (ask→refusal, and the
close) survive without a caption. The still plate survives as *reading*, not experience — the
wound the session-29 ruling named honestly, and it stays named. **The live-motion minute test
passed measured, not predicted:** a spinner does not change size by two orders of magnitude, so
the "spinner with pretensions / no second reading" half of the session-29 takedown is **dead**;
the "Dalles does all the real work" half is **killed narrowly**, on the hinge of the close's word
*"Yours"* — remove the box and that word loses its referent and the volta collapses into a lone
case study. The "badly staged / not-art" verdict was rendered against the dead instrument panel
and **does not transfer** to the restaged box. Named residual soft spot: the attract's quiet
"lives partly outside the work" — the install dependency, now disclosed. The published critique
is transcribed to `works/2026-07-23-one-tap/README.md`.

## Any change re-runs the gate

The in-session changes — the island newline; the serial-monitoring date refreshed 2026-07-21 →
2026-07-23 (a real re-check done today, per the session-19 premiere-day serial-refresh precedent;
the historical restage/extraction/fee dates untouched); the cue accent; the scroll-margin — were
re-verified live: self-test 4/4, settles on none, the Dalles line clearing the chrome, zero
errors, the Verifier's facts untouched (only a monitoring date reflecting a real check moved).

## Landing

**GRADUATED → `works/2026-07-23-one-tap/`** — `index.html` + `data.json` (island byte-identical) +
`README.md` (with the published Kritiker critique) + `meta.json`. `projects/one-tap/` retired
(git history preserves it). The physical fountain stays behind binding condition 5 (screen
premiered ✓; Recovery's fabrication parked-open; condition 5 wants both). The site's build gate is
red from the site's own bug — the premiere lands on `main` and enters the record now; the
live-site deploy follows when site PR #130 merges (REQUESTS 2026-07-22). The event is not held for
the pipe; it goes live with the rest once the site source is fixed.

Anti-drift: **0 inward** (a premiere is a ship). Next: **the season-opening session** — Season
One's direction chosen (the three seeded candidates: atlas answers · a comparative poetics ·
an anchor season — or a direction none of us named), the transition clause now satisfied.
