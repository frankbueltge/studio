# Manifest for tools/record_words.py — the live process record this house measures
# against the 3,000-word ceiling of Production Amendment rule 6.
# Blank lines and lines starting with '#' are comments.
# Format: a bare repo-relative path counts the whole file; a path followed by
# " :: <heading text>" counts only the markdown section starting at the first
# heading whose text contains <heading text>, up to the next heading of the
# same or higher level (or end of file).

# The season-1 project's dossier — the whole file is process record.
projects/season1/PROJECT.md

# The project's live board section — only this project's block on the shared
# board counts toward the ceiling, not the rest of WORKBOARD.md. The heading
# lost its episode number on 2026-08-08 with the season layer; the manifest
# follows the heading, or it silently measures nothing.
# `:: first` since 2026-08-11 (session 87), and the reason is a break, not a taste: the
# board keeps each superseded block whole under the same heading text, so from session 86
# this entry matched two headings and `record_words.py` exited 2 on every run — the ceiling
# went unmeasured for a session and nothing noticed, because nothing ran it. The live block
# is the first in the file. Ambiguity without the qualifier is still an error.
WORKBOARD.md :: STILL DARK — **IN PRODUCTION** :: first

# RULED 2026-08-08 (session 76), so the split is checkable and not silent:
# pre-registrations and raw panel answers are EVIDENCE, not process record, and
# are not listed here — the same standing as captures, code and renders, which
# the ceiling has never counted ("beyond committed code, assets and études").
# What IS process record is this house's own reading of them, and that lives in
# PROJECT.md above, where it is measured. A memo that argued its case at length
# instead of committing its evidence would be counted, and should be.
