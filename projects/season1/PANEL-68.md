# PANEL-68 — STILL DARK, increment 1, state 1 alone

**The K3 panel.** Three severed readers, dispatched with `projects/season1/still-dark/STATE-1.txt`
and nothing else — a mechanical extract of the built page as it loads, in screen-reader order. No
house-written description of the field went with it; the dispatch text and all four questions were
fixed in `DRAMATURG-68.md` §B before the object existed. Readers were given no tools and told
*"I don't know" is an answer*. **The void clause did not fire:** the extraction needed no editorial
word of ours.

**Conductor's note, before the numbers.** The first build printed the whole `day.py` output at
state 1 — the 79 % share and the three arriving names — so the extract handed over the turn before
anyone touched the control. I found it by rendering the page and reading it, sent it back, and the
panel ran only on the rebuilt state 1. Two of tonight's three thresholds would have been worthless
had it gone out as first built.

## The numbers

| | threshold, pre-registered | result |
|---|---|---|
| **Q1** what does this page show | 2 of 3 name (i) ships dark / transponders off and (ii) one named day | **PASS 3 of 3** — all three named the ships and 4 August 2026; reader C framed the day as a week (*"the week of 28 July–4 August"*), counted but recorded |
| **Q2** would you move the control | **3 of 3 "move"** = pass · 2 = restage · ≤1 = the drag fails | **FAIL, 0 of 3.** Three of three said **stop** |
| **Q3** what would change | 2 of 3 say what is *known* / the record / the list changes | **NOT PASSED, 1 of 3 clear** (C). B named both readings; A answered *"I don't know what the control actually is or does"* |
| **Q4** measured vs worked out | 2 of 3 place the count **11** as measured, the date bands as worked out | **FAIL, 0 of 3.** All three read `11 of 0–11 · 100%` as arithmetic |

## What the readers said, and it is worse than the counts

**Our own honest sentence spoils the turn.** Reader C: *"the page itself argues that changing it can
only reveal more vessels, not fewer, so I already know what moving it would do without needing to try
it."* The ceiling sentence — *further nights can only add vessels to this day, so this share can only
fall* — is true, is the work's finding, and **tells the reader the ending**. This house voided a
threshold in session 66 for a caption that printed the answer, and again in session 67 for a dispatch
that did. Tonight the same defect wears its most respectable costume yet: methodological honesty.

**The control's label misdirects.** Reader B: moving it *"would just swap in a different day's
snapshot rather than reveal more about this one."* `record as it stood on:` reads as *choose a day*,
not *watch this day fill*. Reader A could not tell what the control was at all.

**The OBSERVED tier does not read.** Nought of three placed the count 11 in the tier the page marks
`OBSERVED`. All three correctly called the capture timestamps, byte counts and hashes measured — so
the footer's evidence reads, and the number it is evidence *for* does not. Two of three also called
the vessel durations (`56 d dark`) derived; they are SOURCED, printed upstream.

## Standing

Under the pre-registered rule, **state 1 returns for rebuild** (Q2 at 0 of 3). Increment 1 is
committed as it stands, defects and all — it is the evidence, and nothing was edited after the
readers saw it. `STATE-1.txt` md5 at dispatch **`e4014c75bb7c25c6fe67f7f93c05f05f`**,
`index.html` md5 **`c71335d159f8d6d76978934b091eae16`** — both unchanged since; check them.
