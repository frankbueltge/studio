# DRAMATURG-96 — STILL DARK, 4 August 2026, the seventh premiere gate

**Object hashed by me before I looked at anything:**

```
e0f41e9105658901b03f1653df45e9f9a6963c780a1374a7582fe29cc44bad0b  projects/season1/still-dark/index.html
2d11294240cc056b29c1d969109ad8088f913269  (HEAD, research/session-2026-08-15-2)
```

**I moved nothing.** Every instrument I ran is read-only; every scratch file, screenshot and
instrument of my own is under `/tmp/dr96`. Not `render.mjs`, not `data.py --write`, not
`tools/live.py --write`, not `capture/capture.py`. The only file I have written in this repository
is this one. Two other memos of tonight's gate appeared in the working tree while I measured
(`KRITIKER-96.md`, `VERIFIER-96.md`); I did not open either, for the same reason my predecessor at
session 91 did not.

---

# VERDICT: DELIVERS WITH CONDITIONS

**The block that arrived tonight belongs, and it is the first time in seven gates I can write that
sentence about something this house added rather than something it cut.** `section.sd-compare` is
6.6 % of the page at 1400×900 and 7.2 % at 390×844. It stands 4,630 px from the reserved emptiness
and takes nothing from it — `frame.mjs` reads the hole at 309 px and 26 of 38 chips at 390×844
tonight, the same numbers it read last night. It does not touch the ending: 871 px of evidence
stands between it and `.sd-floor`, and nothing at all stands between the floor and the bottom of the
page. It introduces no typographic device this page did not already own — I checked all four of them
against their originals. And it arrives at the one moment in the arc where it can do work: directly
after the section where this page strikes through its own earlier published figure, and directly
before the section where the machine's raw output stands verbatim. This day → this day struck
through → four days of this sea → the instrument's own bytes → the refusal. That is an escalation
and the new block is the widening step in it.

**It is not a trial register.** The constitution's words are *"a table may appear inside a work,
never BE the work"*, and this is 428 px of 6,532 on a page that already carries a larger table.
The charge fails on the measurement, not on a plea.

**Three cuts below are BLOCKING and none of them is about whether this ships.** Under the
architect's amendment this verdict does not hold the premiere; it travels with the work. So I have
written every item at the size the measurement gives it and narrowed nothing to fit. Three of my
predecessor's six items are discharged tonight, one is voided legitimately, and two are unpaid and
re-issued under my name.

---

# § THE SIX ITEMS I INHERITED

**Cut 1 — the caveat's rank. PAID in the limb that made it blocking. Its height limb is unpaid and
stands as my cut 1.** Measured, alpha-weighted, on the 1400×900 first screen at rest, against
last night's file driven beside tonight's in the same run:

| `#sd-arrive-hedge` at 1400×900 | last night (`1dae228`) | tonight | the figure, tonight |
|---|---|---|---|
| computed size | 12.48 px | **11.52 px** — equal to the premise | 46.4 / 24 / 15.2 px |
| computed ink | full — `rgb(17,17,17)` | **`rgba(0,0,0,0.55)`** | full |
| inked glyph area, weighted by alpha | **82,200 px²** | **38,971 px²** | 46,183 px² |
| share of all ink on the first screen | **41.5 % — first place** | **25.1 % — second place** | **29.8 % — first place** |
| height | 168 px | 155 px | 145 px |

**The caveat's perceived weight fell by 53 % and it stopped being the heaviest object on the screen
this work opens on.** I drove it and I looked at it at both schemes. The reserved 135 px now reads
as a void that belongs to the material above it; last night it read as a gap between two blocks with
the lower one heavier. That was the sentence that made the cut blocking and it is no longer true.
The house paid it without touching one clause, which is what the order asked for.

**Cut 2 — the apposition. PAID, in the second form the order permitted.** `", N days after the day
had ended"` prints at exactly one stop, `+1 DAY`, and nowhere else; the other eleven end on a bare
edition date. Re-measured below, with its cost priced.

**Cut 3 — the frame's thirteen nights. VOIDED, and the voiding is legitimate — it is a better answer
than the one I would have ordered.** The cut asked for a lamp under a fuse. The house cut the fuse:
`data.py` loads captures as of a fixed instant, so no later list adds a button, a chip or a beat to
this face; the work states its window and its count on its own face; `ADDENDA.md` exists and is
real. That is the architect's own rule of 2026-08-15 and it disposes of the item on its own terms,
not by narrowing it. **One sentence of residue, and it is a note and not a cut:** the freeze is a
policy, not a lock. `FREEZE_AS_OF` is one constant, `frame.mjs` still publishes no headroom figure,
and a successor that unfreezes gets thirteen nights back at 1400×900 with no lamp under them. The
place for that sentence is `ADDENDA.md`, which already tells its successor not to touch the
constant.

The other half of that cut — **320×568 joins the four** — is paid, and it returned information
nobody in this house had. It is my cut 4 and my cut 5.

**Cut 4 — `turn.mjs` reports node boxes and calls them motion. NOT PAID.** It holds nothing at this
gate: no cut of mine rests on its unit and no element of the staging depends on it. It is re-issued
as my cut 6 with tonight's re-measurement, because it has already cost this work a sentence on
arithmetic that was wrong by an order of magnitude, and the same arithmetic is still in the guard
table.

**Cut 5 — the false ground of the standing order. CORRECTED**, in `data.py`'s comment and in
`README.md`, with the working reprinted and the order left standing. Discharged. **Cut 6 — the dead
selector. REMOVED**; `frame.mjs` prints no line about `#sd-arrive-constant` at any viewport tonight.
Discharged.

---

# § NUMBERED CUTS

### 1. `#sd-arrive-hedge` is still 155 px against the hole's reserved 135, and no voice at this gate but the fact voice can shorten it. **Not blocking.**

**What I measured.** The third conjunct of my predecessor's own criterion: *"its height at 1400×900
not greater than the reserved height of `#sd-arrive-names-since`"*. **155 px against 135 px.** It is
still the tallest single element on the 1400×900 first screen, and it still carries 25.1 % of that
screen's ink for a footnote. Its trajectory at 390 is 75 → 150 → 318 → **276 px**; tonight is the
first night it has come down.

**I decline to make this blocking, and I want the reason on the record rather than inferred.** The
measurement that made it blocking is gone: it no longer outranks the premise, it is no longer the
brightest thing on the page, and the void above it reads. Twenty pixels of grey commentary is not
the defect that returns a work.

**But the brief asks exactly what more must go and who may order it, so here it is, clause by
clause.** The paragraph is four sentences, 148 words:

1. *"A list gives a ship's return only to the nearest week, so a name counts as certain here only
   when every day of that week leaves it dark on this one; the rest are possible."* — 32 words. It
   defines *certain*, which is the word the turn turns on. **Mine to protect, not to cut.**
2. *"No name this record could already have on the day itself can be certain: the earliest end of a
   certain name's week falls seven days before the list that first printed it, so its first list is
   dated at least a week after the day."* — 44 words. Its **finding** is the disjointness result of
   session 92's gate and is printed nowhere else. Its **ground** is the last 30 words, and I counted
   the seven-day window printed on this face in six places: this paragraph, `#sd-arrive-since-note`,
   `#sd-arrive-cut-refused`, `#sd-bandnote`, `#sd-ledger-cap` and `method.definition`. This ground
   was installed by `VERIFIER-93` blocking 2, which replaced a false one. **Only a fact voice may
   withdraw it.**
3. and 4. — 72 words, and they are `day.py`'s own `share_band_condition`, carried onto the face
   verbatim by `data.py:906` so that the sentence qualifying this page's largest number cannot go
   stale against the instrument that produces it. **Only a fact voice may touch them.** They are
   also printed a second time on this face, verbatim, inside `#sd-raw-pre` at y 6219 — which is not
   a duplication a staging voice may cut, because that block's whole contract is that it is the
   instrument's output unedited.

**So the answer to "who may order it" is: nobody at this gate but the fact voice, and only on
clauses 2, 3 and 4.** Which leaves the staging voice exactly one lever, and it is not the words —
it is the place. This page has an established destination for prose that is owed but not staged:
below the controls, with this head's other prose, where four paragraphs already went in sessions 85,
87 and 93. `DRAMATURG-90` refused that move on the ground that *the key must stand near the thing it
unlocks*; I note the refusal, I do not overturn it, and I name the new evidence that would let a
successor: the block now carries a quarter of the first screen's ink and stands on the lower edge of
this work's one image.

**Testable:** the height of `#sd-arrive-hedge` at 1400×900 is not greater than the reserved height
of `#sd-arrive-names-since` — reached by a fact voice cutting words, or by the paragraph standing
below `#sd-arrive-controls` with this head's other prose. Either discharges it.

---

### 2. The apposition is paid; the falling numeral leads five of six beats and not six of six, and the one beat it loses is not lost to the apposition. **Not blocking.**

**What I measured.** An instrument of my own: the run driven unattended at 390×844 with the page
never scrolled, every text node in the document polled, the common prefix and suffix stripped from
each changed node, the differing run measured with a DOM `Range`, every rectangle clipped to the
viewport. It reports what changes **on the glass at the scroll position the visitor is at**.

| beat | on-screen ink | the falling numeral | largest thing on screen |
|---|---|---|---|
| **1** | 21,955 px² | 1,281 px² — **5.8 %, fifth** | `#sd-arrive-state`, 7,280 px² — **33.2 %** |
| 2 | 1,585 px² | 640 px² — **40.4 %** | the falling numeral |
| 3 | 1,231 px² | 641 px² — **52.1 %** | the falling numeral |
| 4 | 1,529 px² | 640 px² — **41.9 %** | the falling numeral |
| 5 | 2,303 px² | 1,281 px² — **55.6 %** | the falling numeral |
| 6 | 2,460 px² | 1,281 px² — **52.1 %** | the falling numeral |

**Four of my predecessor's five predicted figures reproduce to the decimal** (52.1, 41.9, 55.6,
52.1). Beat 2 comes in at 40.4 % rather than the predicted 52.0 %, because that scratch copy had
stripped the apposition from every stop and this object keeps it at one — so at beat 2 the tail is
still deleting it, and my unit under-reports a deletion by construction. I name that limit rather
than bank the number.

**Beat 1 fails the criterion and it is not the apposition's fault.** The largest change on the first
screen at the moment the run begins is `#sd-arrive-state`, the run announcing itself — 7,280 px²,
33.2 % — with the hole's heading second at 4,403 px² and the apposition third at 4,144 px². The
falling numeral is fifth at 5.8 %. That is the run's own voice speaking after fourteen seconds of
silence, and I would not cut it to win a ratio. **The cut is paid in substance and its literal
criterion fails at one beat for a reason the cut was not about.** It goes in the register at that
size and no larger.

**What the cut cost, priced and not predicted.** Two numbers, both taken in the page's own
arithmetic.

- **The run still teaches, and the teaching happens in the dwell, not at beat 1.** At stop 0 the
  clause reads *"counting the lists up to 4 AUG"* with `ON THE DAY` lit beneath it, and it stands
  for 14,118 ms. `<h1>` says `4 AUGUST 2026`; the buttons say `+1 DAY … +11 DAYS`; the clause says
  `5 AUG … 15 AUG`. The mapping is arithmetic a reader completes without being told, and the one
  instance of the apposition confirms it at the easiest step. **The eleven bare dates cost the run
  nothing it was carrying.**
- **The one instance is on screen for less time than this page's own cited rate says it takes to
  read.** *", a day after the day had ended"* is 8 words. At 238 wpm — the rate this page cites
  (Brysbaert 2019) and budgets its own 14,118 ms dwell against, 57 words at 14.4 s — those 8 words
  take **1,765 ms**. The beat is **1,600 ms**. It is 165 ms short, and it arrives in the most
  crowded 1.6 s of the whole run: six nodes rewriting, 21,955 px² of on-screen ink, nine times the
  next beat's.

**Testable, if a successor wants the residue paid:** the apposition stands at a stop whose dwell is
not shorter than its own reading time at the rate this page cites for its dwell.

---

### 3. `— THE DAY THIS PAGE PUBLISHES` is fastened to the numerals and not to the day it names, and the numerals it is fastened to are not the ones this page publishes. **BLOCKING.**

**What I measured.** Every printing of both bands on the face, with position, size, weight and ink:

| | y | size / weight | label above or beside it |
|---|---|---|---|
| `.sd-share-fig` | **4,540** | **27.2 px / 700**, full ink | **LIVE** — *as this record measures it now* |
| `.sd-compare-share` | **5,126** | 15.2 px / 700, full ink | **— THE DAY THIS PAGE PUBLISHES** |

`22 %–38 %` and `31 %–73 %`. **Both are 4 August 2026. They stand 586 px apart. The one marked as
the day this page publishes is not the band this page publishes, and it is set at 56 % of the size
of the one that is.**

Neither number is wrong and the section's premise is exactly why they differ — `31 %–73 %` is that
day read at eight days old, which is the whole point. The fault is placement. The phrase is a mark
**on the day**; the day column reads `4 August 2026` and would carry it perfectly. Instead it is
appended to `.sd-compare-frac`, at the far right of the row, in the only display-weight full-ink
capitals in the section, adjacent to nothing but numbers.

**And in the linear reading it is worse, which is the measurement that makes this blocking.** The
committed screen-reader record, `STATE-1.txt:421–423`, prints the row as:

```
4 August 2026
31 %–73 %
11 of 15–35 — THE DAY THIS PAGE PUBLISHES
```

A listener hears the day, then a band, then a fraction, and *then* is told this is the day this page
publishes — the mark arriving last, as a coda on the numbers rather than a label on the date. The
stylesheet's own comment at `index.html:781–782` says the mark exists in words *"and not by colour
or weight alone: a mark the ear cannot hear is a mark half this page's readers do not get"*. The ear
hears it. It hears it attached to the wrong thing.

**What must change.** The mark moves into `.sd-compare-day`, beside the date it marks. Nothing is
added, nothing is deleted, the screen-reader payload is unchanged, and the row reads *"4 August 2026
— THE DAY THIS PAGE PUBLISHES · 31 %–73 % · 11 of 15–35"* in both media. This is a cut of position
and it is the whole repair, exactly as `DRAMATURG-91` cut 3 was.

**Testable:** the element carrying the string `THE DAY THIS PAGE PUBLISHES` is a descendant of
`.sd-compare-day`, and no descendant of `.sd-compare-share` or `.sd-compare-frac` carries it.

---

### 4. `tools/frame.mjs` prints a breach of its own published floor and then prints a green summary and exits 0. **BLOCKING.**

**What I measured.** `NODE_PATH=… node tools/frame.mjs`, exit code taken without a pipe: **0**. Its
first block:

```
small phone 320×568 — figure-top to controls-bottom: 411 px of 568 — HOLDS
  the hole sharing a frame with the whole figure: 0 px, 0 of 38 chips at the last stop
      — floor 268 px / 22 chips — UNDER
```

and its last line:

```
FRAME: the figure and the controls fit one screen at every stop
```

**A guard printed `UNDER` against a floor this house published, on the viewport a gate ordered it to
watch, and then printed a sentence with no qualification in it and exited green.** The file's own
comment at line 249 says the hole is *"Reported, never enforced"* — written when the only viewport
carrying a floor was the one that held it. It is no longer that. `frame.mjs` is the report this
house reads its floor off; six memos including mine quote its numbers; and a summary line that
cannot say *except at 320×568, where the hole is nought* is a summary that will be quoted as a pass.

This is the same species of defect as `DRAMATURG-94`'s cut on the run — an alarm that had become a
gauge — and this house fixed that one in a night.

**What must change.** Either the floor is enforced at every viewport that carries one and the exit
code follows, or the report says in its own words why 320×568 is exempt and the summary line names
the exemption. What may not stand is a breach printed in the body and a green sentence at the foot.

**Testable:** `frame.mjs`'s final line and its exit code account for every `UNDER` printed above
them.

---

### 5. At 320×568 the figure and the reserved emptiness never share a frame — 0 px, 0 of 38 chips. **Not blocking, and it must be published as a fact.**

**What I measured.** The document at 320×568, at rest and at every stop. The order swap at ≤ 480 px
paints the controls directly under the figure, so figure-top to controls-bottom is 411 px of 568 and
holds comfortably. The hole is at **y 903–1,429**. The figure is above the controls at the top of
the document. **They are 438 px apart in a 568 px window and the instrument is right: at this size a
visitor watching the number fall is watching it with the work's one image entirely off the glass.**

At 390×844 the same reading is 309 px and 26 of 38 chips against a floor of 268 and 22 — HOLDS,
identical to last night, untouched by tonight's build.

**I decline to block it, and the reasons are three.** The floor was set at 390×844 by
`DRAMATURG-89` and holds there. 320×568 is a 2016 phone and no gate before session 95 had it on any
list. And the head prose standing between the figure and the hole at that width — 145 px of premise,
135 px of names, 59 px of the hole's heading — was ordered onto this page clause by clause by three
fact voices and two staging voices, so the only cheap payment would be unbuilding other voices'
conditions to win a span, which is the trade `DRAMATURG-89` cut 1 exists to stop.

**This is new information nobody in this house had and it should be published in those words**: the
work's central staging relation does not exist below some width between 320 and 390, and the house
has never known where that boundary is. **Testable, and cheap:** `frame.mjs` walks the width at
which the hole first shares a frame with the figure and prints it, the way `width.mjs` walks
280→1920 for the sideways scroll. Then the boundary is a number in the record instead of a surprise.

---

### 6. `tools/turn.mjs` still reports six nodes where twelve differ, and still calls a box a motion. **Not blocking. Re-issued from `DRAMATURG-95` cut 4, unpaid.**

**What I measured.** Tonight, on the same beat, at the same width. `turn.mjs` at 390, stop 10 → 11:

```
6 nodes rewritten, 34440 px² in motion
   15308 px²   44.4 %   the hole's heading
```

The same beat, measured as changed glyphs by my instrument: **twelve text nodes differ** — the two
ends of the share, the hole's heading, the count that turns, both divisions, the when-clause's tail,
the caveat, the proof command and three new chips — **7,445 px² of ink in the whole document, of
which the hole's heading is 432 px², or 5.8 %.** Not 44.4 %.

Both units are proxies and I claim nothing more for mine than my predecessor claimed. But the node
count is not a proxy: it either equals the number of nodes whose text differs or it does not, and it
does not. This instrument's arithmetic has already bought one sentence's deletion on a figure that
was off by an order of magnitude (`DRAMATURG-94` cut 4), and the same figures are still in the guard
table this house publishes.

**Testable:** the printed node count equals the number of differing text nodes, and a changed-glyph
figure stands beside the box figure.

---

### 7. The four days arrive behind 155 words of grey — 39 seconds at this page's own cited rate. **Not blocking.**

**What I measured.** Everything a reader passes between the fall section and the first numeral of
the new one:

| | words | size / ink | height at 1400 / 390 / 320 |
|---|---|---|---|
| `#sd-frozen` | **80** | 11.52 px, 55 % | 119 / 208 / 244 px |
| `#sd-compare-head` | 15 | 9.92 px bold, full ink | 14 / 28 / 28 px |
| `#sd-compare-lead` | **60** | 11.52 px, 55 % | 71 / 125 / 143 px |
| **before the first `.sd-compare-share`** | **155** | | **204 / 361 / 415 px** |

**155 words is 39.1 s at 238 wpm** — the rate this page cites and budgets itself against. The head
sets its premise at 57 words and reserves 14,118 ms for it, which is 14.4 s at the same rate: **the
head budgets its own preface to within 300 ms of the rate it publishes, and this section's preface
is 2.7× the head's and budgeted by nobody.**

**Two candidates, named because both say things the reader can already see.**

- `#sd-frozen`'s third sentence, 39 of its 80 words: *"The instrument keeps publishing daily and
  this record keeps saving it; every list that arrives after that instant is published as a dated
  addendum beside this work, in ADDENDA.md, and none of them moves a number on this face."* The
  freeze rule requires the work to state **its window and how many sources it holds** — that is the
  first two sentences, 27 words, and they are on the face. The rest is a policy note, and it names a
  file **in plain text on a page that carries 52 links, every ship name among them.** A destination
  a reader cannot reach from the page that names it is inert by the plainest definition.
- `#sd-compare-lead`'s third sentence, 14 words: *"Every row is the same committed script, run with
  the instant printed under it."* Every row prints `$ python3 …/day.py <day> --as-of <instant>`
  underneath itself. This is the sentence describing the thing the reader is looking at.

**Testable:** the words standing between `.sd-fall` and the first `.sd-compare-share` fall below
120; and no sentence in `#sd-compare-lead` describes what a `.sd-compare-row` prints.

---

### 8. The documents that travel say the work lacks the block it has, and point at a register that does not exist. **Not blocking, under the amendment's own second rule.**

`README.md:1199–1201`, written tonight: *"**OWED, and named rather than softened:** the critic's
**condition 2** — the face must show what the same measurement returns for another day of this same
record."* At the hash above, the face shows it in 215 words and 428 px. The README's newest section
is headed `THE STATE OF EVERY GUARD, SESSION 95`; a stranger reading the exposition that ships
beside this object meets an account of a face one build behind the one they are looking at.

`ADDENDA.md`, last paragraph, directs its successor to *"the register beside this file,
`OPEN-DEFECTS.md`"*. **There is no `OPEN-DEFECTS.md` in this directory at the hash above** — at a
gate whose named decision is to premiere with a published register of open defects, on the night my
own unpaid items are supposed to land in it.

**I do not block either.** The amendment says blocking verdicts attach to face, instrument, README,
exposition and apparatus, and it also says errors in the record's prose about its own nights are
banked and do not hold a premiere. The README paragraph is prose about session 95's gate; that is
squarely the second rule. The missing register is a file the session may still be writing, and it is
not the object. Both are banked here so neither is found again as new.

---

# § WHAT I LOOKED FOR AND DID NOT FIND

- **A stolen ending.** No. At 1400×900 `.sd-compare` ends at y 5,449; `.sd-evidence` runs 5,464 →
  6,335; the source line is at 6,352; `.sd-floor` is 6,395 → 6,519 in a 6,532 px page. I enumerated
  every element in the document after `.sd-floor` and there are **none**, at all three viewports.
  `DRAMATURG-91` cuts 2 and 3 hold. The finding of the new section is set at 12.48 px / 400 in a
  left rule; the floor is 18.4 px / 700 above a full rule. The louder sentence is still the right
  one, and it is still *"No number closes this."*
- **A fifth visual idea.** No, and I checked each of the four against its original rather than
  taking the stylesheet comment's word. `.sd-compare-share` + `.sd-compare-frac` is `.sd-share-fig`
  + `.sd-share-of` from the fall section, at half the size. `.sd-compare-finding`'s
  `border-left: 2px` is the device already carried by `.sd-quoted`, `.sd-fell` and
  `.sd-group-late .sd-rows`. `.sd-compare-row`'s hairline is the ledger's. `.sd-compare-check`'s
  `$ …` in `<code>` is the proof block's. **Nothing new was introduced typographically, and the row
  is the head's own frame compressed to one line: a day, a share, its division, the command that
  returns it.**
- **A weakened image.** No. The hole is reserved before it is filled, its heading still says nobody
  could have had this, and `frame.mjs` reads it tonight at 135 px / 38 of 38 chips at 1400×900 and
  309 px / 26 of 38 at 390×844 — the same as last night, at a page 603 px longer. **A work that can
  absorb a whole new section without moving a pixel of its one image has been built correctly.**
- **A broken first encounter.** No, and tonight's build did not touch it: `#sd-arrive` opens at
  y 85 at 390×844 exactly as it did last night. The first screen still holds the date, the subject
  line, the 57-word premise, `100 %–100 %`, `11 of 11`, `0 of them certainly dark`, `11 of 230`,
  thirteen buttons, the run's line, the eleven names, the hole's heading and the top of the reserved
  emptiness — then fourteen seconds of nothing. It passes, and it passes on the phone.
- **Motion in the new block.** None, and I looked for it in every beat of the run: no node in
  `#sd-frozen` or `.sd-compare` appears in any beat's diff. It is honest that they are still — they
  are a fixed comparison — but it is worth saying plainly that the section paying this work's
  *repetition* limb is the one block on the page a person with ordinary time could have typed. What
  redeems it is that every row carries a command, so the repetition is something a stranger
  performs rather than reads.
- **The strongest thing in this section, which the staging leaves on the floor.** I am naming it and
  not ordering it, because ordering an addition is co-writing. **The four instants at which the four
  days are read are the last four buttons of this work's own ladder.** Row 1's
  `--as-of 2026-08-12T18:23:12Z` is byte-identical to stop 8, `+8 DAYS`, whose share is `31 %–73 %`
  and whose fraction is `11 of 15–35` — the row's two numbers exactly. Rows 2, 3 and 4 are stops 9,
  10 and 11. A visitor could press `+8 DAYS` and see the table's first row in 46.4 px type. The page
  prints those four instants twice, 4,000 px apart, and never says they are the same four instants.
  It is a free connection between the work's one interaction and its one generalisation, and it is
  not made.
- **Slop.** None. No gradient, no icon, no emoji, no easing, no colour beyond ink and one grey; both
  schemes painted explicitly, and I read every block's computed colour in both. `width.mjs`
  **CLEAN**, 280→1920. `gaps.mjs` **PASS / PASS**, 0 colliding tick labels at seven widths.
- **A responsive failure in the new block.** None. `.sd-compare-row` reflows to `1fr auto` at
  ≤ 30 rem with the fraction spanning; the rows measure 342 px at both 390 and 320, so nothing
  re-wraps between them, and DOM order equals visual order at every width. I looked at it at 1400,
  390 and 320.
- **A stale apparatus.** None. `RENDERS.json` carries `index_sha256`
  `e0f41e91…`, byte-identical to the object I hashed, and `render-1400.png` is 1400×6532 — the page
  height I measured tonight to the pixel. `STATE-1.txt` carries the freeze line and all four rows.
  The pictures and the screen-reader record are of tonight's page, which is not something three
  earlier gates could say.
- **What the night cost the face, measured on last night's file driven beside tonight's in one run:**
  page height **5,929 → 6,532 px** at 1400 (+603, +10.2 %), **9,083 → 10,062** at 390 (+979,
  +10.8 %), **10,755 → 11,838** at 320 (+1,083, +10.1 %); visible words on the face **3,351 →
  3,691** (+340, +10.1 %), of which 295 are the two new blocks and the rest is the numerator's
  direction, paid to `KRITIKER-95` condition 1. **Not one of those pixels came out of the head:**
  `#sd-arrive` is 1,374 → 1,361 px at 1400 and 2,604 → 2,562 at 390 — it got *smaller*, by exactly
  the caveat's type step.

---

# § WHAT I DROVE

A browser automation library's Chromium, launched by me, against the frozen file at `file://`, at
1400×900, 390×844 and 320×568, in both colour schemes. Ten screenshots and four page strips, all
looked at. Everything under `/tmp/dr96`.

**Committed instruments, all run by me, exit codes taken without a pipe:**

| | exit | reading |
|---|---|---|
| `tools/frame.mjs` | **0** | HOLDS at five viewports; hole 135 px / 38 of 38 at 1400×900, 309 px / 26 of 38 at 390×844 — HOLDS; **0 px / 0 of 38 at 320×568 — UNDER**, and the summary is green (my cut 4) |
| `tools/width.mjs` | **0** | CLEAN, 280→1920, no width holds a document wider than its window |
| `tools/fold.mjs` | **1** | 13 places, 156 sightings, zero occlusions — the standing published red |
| `tools/turn.mjs` | **0** | 6 nodes, 34,440 px² at 390 and 45,681 px² at 1400 (my cut 6) |
| `still-dark/gaps.mjs` | **0** | PASS / PASS, 0 colliding tick labels at seven widths |
| `still-dark/announce.mjs` | **0** | 1 live region, 4 writes, 3 spoken, 13 figure rewrites, ceiling 45,000 ms against a run of 31,718 ms with 13,282 ms of room, the promise and the run agreeing, the closing line carrying `22 %–38 %` |

**Read directly:** `index.html` and its data island parsed and tabulated, `data.py`'s hedge
construction and freeze, `ADDENDA.md`, `RENDERS.json`, `STATE-1.txt`, `README.md`'s head and its
session-95 section, `PROTOCOL.md` entire, `DRAMATURG-95`, `DRAMATURG-91` and `KRITIKER-95`. Last
night's object extracted with `git show 1dae228:… > /tmp/dr96/prev/index.html` and driven beside
tonight's by the same script in the same run. **Not run, per the pass rules:** `render.mjs`,
`data.py --write`, `tools/live.py --write`, `capture/capture.py`.

**My own instruments, written tonight under `/tmp/dr96` and thrown away with this memo:**

- **`ink.mjs`** — the unattended run polled every 90 ms, every text node in the document diffed by
  common prefix and suffix, the differing run measured with a DOM `Range` and clipped to the
  viewport at scrollY 0. Cuts 2 and 6 rest on it. It under-reports deletions and I have said so
  where it matters.
- **`weight.mjs`** — every block of the first screen as total inked glyph area and as that area
  multiplied by its ink's alpha, at two viewports, run against both tonight's file and last
  night's. Cut 1's table is its output.
- **`register.mjs`** — height, width, word count, computed size, weight and colour of nineteen
  blocks at three viewports in both schemes.
- **`cost.mjs`** and **`words.mjs`** — the two files driven side by side for page height, section
  positions and visible face words.
- **`bands.mjs`** — every printing of `22 %–38 %` and `31 %–73 %` on the face with its position,
  size, weight and ink. Cut 3 rests on it.
- **`full.mjs`**, **`strips.mjs`**, **`shot.mjs`**, **`text.mjs`**, **`links.mjs`**, **`probe.mjs`**
  — the tail of the document enumerated after `.sd-floor`, the page cut into readable strips, the
  screenshots, the whole rendered face dumped with positions, the anchors, and the run sampled at
  1 Hz to confirm the apposition prints once.

**A measurement I did not take, and a successor should.** Whether the boundary width at which the
hole re-enters the figure's frame lies at 340, 360 or 380 px. I know it is between 320 and 390 and
I did not walk it, because walking it is `frame.mjs`'s job and I have ordered it to (cut 5).

---

**index.html, sha256 at the START of this pass:**
`e0f41e9105658901b03f1653df45e9f9a6963c780a1374a7582fe29cc44bad0b`

**index.html, sha256 at the END of this pass:**
`e0f41e9105658901b03f1653df45e9f9a6963c780a1374a7582fe29cc44bad0b`

**`HEAD` `2d11294240cc056b29c1d969109ad8088f913269` at the start and at the end**, on
`research/session-2026-08-15-2`. **Neither moved under me and I did not move either.** `git status`
is what I found it, plus this file and the two memos other voices wrote while I measured.

---

**THE SENTENCE A SESSION SHOULD PRINT ABOUT THIS GATE:**

> On the seventh attempt this house stopped grinding rulers and built the thing it was asked for —
> four days of one sea read at one age, differing by a factor of ten, in the page's own grammar, in
> the one place in the arc where it does work, costing the reserved emptiness not a single pixel.
> What it then did was fasten the words *the day this page publishes* to a band this page does not
> publish, 586 px below the band it does; and its own frame guard, at the phone a gate told it to
> add, printed that the work's one image and the figure it belongs to never share a screen — and
> exited green.
