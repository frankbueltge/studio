# Build feedback 2026-08-16

The build gate is red. This letter does not say whose defect it is, because that cannot be derived from the log: a file in the site repository can fail on data from yours — a test here asserting over your chronicle is red when a heading or an entry is missing upstream, and the path it names is ours either way. What failed is quoted below; read it and judge. If nothing in it touches your work, there is nothing on your side to correct.

Run: https://github.com/frankbueltge/frankbueltge.de/actions/runs/31942891958

Failing lines, verbatim from the validation log:

```
     × carries the eye’s three returns of One Tap, in order, with its own words 14ms
     ✓ every return record is really a span of the chronicle it names 8ms
     ✓ agrees with the season floor about how many times the eye sent a work back 17ms
     × refuses the title-substring rule the committed record would defeat 3ms
     ✓ gives one chronicle entry one row, however many rules reach it 1ms
     ✓ carries a borrowed evening by the sentence, never by the whole session 1ms
     × keeps every mark: one per premiere, one per strike, one per return 13ms
     ✓ reads WITHDRAWN off the work's own meta.json, not off a hand-kept list 2ms
     × finds the three returns in the chronicle's own sentences, numbered in order 3ms
     × carries each return's own words verbatim, and each record is really in the chronicle 6ms
     ✓ keeps every strike reason and source verbatim from the curated list 1ms
     ✓ names every file it read, so the figure can state its own provenance 1ms
     × marks a strike whose evening the mirror does not carry — never bridges it silently 13ms
     ✓ refuses to draw a season with no chronicle at all 1ms
     ✓ takes a second withdrawal, and a work with no returns, without special-casing either 1ms
     × the headline is NOT the raw maximum — the maximum is the ceiling artefact 10ms
     ✓ publishes the evidence for the standout whenever it publishes a standout 0ms
     ✓ the anniversary is arithmetic on the file itself, checkable without a calendar 1ms
     × every quote is a byte-exact substring of the file it names 13ms
     ✓ has the five scenes the tour promises, each with substance to back it 2ms
     ✓ quotes only files it also declares as provenance 2ms
 FAIL  src/lib/invoked/contract.test.ts > invoked latest.json contract > the headline is NOT the raw maximum — the maximum is the ceiling artefact
AssertionError: expected 1992 to be 2014 // Object.is equality
- Expected
 FAIL  src/lib/studio/dossier.test.ts > buildStudioDossiers over the committed record > carries the eye’s three returns of One Tap, in order, with its own words
AssertionError: expected [ 1, 2, 3, 4 ] to deeply equal [ 1, 2, 3 ]
- Expected
 FAIL  src/lib/studio/dossier.test.ts > attribution — an entry that cannot be attached is omitted, never misfiled > refuses the title-substring rule the committed record would defeat
AssertionError: expected [ 'S28', 'S31', 'S32', 'S43', 'S99' ] to deeply equal [ 'S28', 'S31', 'S32', 'S43' ]
- Expected
 FAIL  src/lib/studio/season.test.ts > buildSeasonModel over the committed record > keeps every mark: one per premiere, one per strike, one per return
AssertionError: expected [ { …(14) }, { …(14) }, …(2) ] to have a length of 3 but got 4
- Expected
 FAIL  src/lib/studio/season.test.ts > buildSeasonModel over the committed record > finds the three returns in the chronicle's own sentences, numbered in order
AssertionError: expected [ 1, 2, 3, 4 ] to deeply equal [ 1, 2, 3 ]
- Expected
 FAIL  src/lib/studio/season.test.ts > buildSeasonModel over the committed record > carries each return's own words verbatim, and each record is really in the chronicle
AssertionError: expected 'The human eye returned One Tap a seco…' to be 'keep working on the staging; this is …' // Object.is equality
Expected: "keep working on the staging; this is even worse staged than the HTML version."
 FAIL  src/lib/studio/season.test.ts > the table floor and the honest gaps > marks a strike whose evening the mirror does not carry — never bridges it silently
```

No deploy happened; the last good state stays live.
