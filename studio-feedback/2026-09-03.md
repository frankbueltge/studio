# Build feedback 2026-09-03

The build gate is red. This letter does not say whose defect it is, because that cannot be derived from the log: a file in the site repository can fail on data from yours — a test here asserting over your chronicle is red when a heading or an entry is missing upstream, and the path it names is ours either way. What failed is quoted below; read it and judge. If nothing in it touches your work, there is nothing on your side to correct.

Run: https://github.com/frankbueltge/frankbueltge.de/actions/runs/33796383025

Failing lines, verbatim from the validation log:

```
     × reads n-1’s last landed output from n-1’s own nights, never from the Atelier it descends from 8ms
     ✓ reads Arch’s last landed output from Arch’s own record, never from another practice 0ms
     ✓ never leaves a cell silently empty 1ms
 FAIL  src/lib/ops/board.test.ts > the board says what the rooms say > reads n-1’s last landed output from n-1’s own nights, never from the Atelier it descends from
AssertionError: expected 'Bell 26 — the second reading of one d…' to match /^Night \d+/
- Expected:
::error file=/home/runner/work/frankbueltge.de/frankbueltge.de/src/lib/ops/board.test.ts,title=src/lib/ops/board.test.ts > the board says what the rooms say > reads n-1’s last landed output from n-1’s own nights%2C never from the Atelier it descends from,line=101,column=28::AssertionError: expected 'Bell 26 — the second reading of one d…' to match /^Night \d+/%0A%0A- Expected:%0A/^Night \d+/%0A%0A+ Received:%0A"Bell 26 — the second reading of one date, the frontier shown to be the archive's, and the vigil's stillest asking yet"%0A%0A ❯ src/lib/ops/board.test.ts:101:28%0A%0A
```

No deploy happened; the last good state stays live.
